---------------------------
--- LOOPER INSTRUCTIONS ---
---------------------------
Compile code
> make all

To run interactively use
> root -b doAll.C
To run in batch mode see README in batchsubmit area

Note environment setup to compile code that is used in 
batch scripts is 

> export CMS_PATH=/code/osgcode/cmssoft/cms
> export SCRAM_ARCH=slc5_amd64_gcc462
> source /code/osgcode/cmssoft/cmsset_default.sh
> source /code/osgcode/fgolf/5.30-patches/bin/thisroot.sh
> export LD_LIBRARY_PATH=$PWD:$LD_LIBRARY_PATH
> export PYTHONPATH=$ROOTSYS/lib:$PYTHONPATH

